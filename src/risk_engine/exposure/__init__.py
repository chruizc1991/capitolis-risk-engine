"""
Exposure aggregation (future work).

Will aggregate per-scenario, per-horizon NPVs from src/risk_engine/simulation/
into Expected Exposure (EE), Potential Future Exposure (PFE), and Maximum PFE
(MPE) profiles, netted by counterparty (the `counterparty` field on each
trade). Empty scaffolding only — not implemented yet.
"""
