"""
Monte Carlo engine (future work).

Will simulate the joint, correlated risk factors from src/risk_engine/models/
forward through time, build a `MarketState` per (scenario, horizon) node, and
reprice the book via the capitolis_pricers pricers at each node. Empty
scaffolding only — not implemented yet.
"""
